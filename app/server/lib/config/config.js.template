var env = process.env.NODE_ENV;
var config = {
  dev: {
    stripeApiKey: '',
    youtubeApiKey: '',
    twitchClientId: '',
    twitchClientSecret: '',
    mailgunApiKey: ''
  },
  prod: {
    stripeApiKey: '',
    youtubeApiKey: '',
    twitchClientId: '',
    twitchClientSecret: '',
    mailgunApiKey: ''
  }
};

if (!App) App = {};

App.config = config['prod'];

if (env === "dev" || env === "development") App.config = config['dev'];