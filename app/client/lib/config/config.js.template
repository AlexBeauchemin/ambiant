var env = process.env.NODE_ENV;
var config = {
  dev: {
    stripeApiKey: '',
    youtubeApiKey: ''
  },
  prod: {
    stripeApiKey: '',
    youtubeApiKey: ''
  }
};

if (!App) App = {};

App.config = config['prod'];

if (env === "dev" || env === "development") App.config = config['dev'];